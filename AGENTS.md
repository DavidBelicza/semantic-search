# Agent Notes

This project plan lives in [docs/go-vector-indexer-implementation.md](docs/go-vector-indexer-implementation.md).

Use that document as the reference for project scope, architecture, and implementation expectations.

Do not code, implement, refactor, or edit project behavior unless the user explicitly asks for that work. Do not treat implementation as implied. If a request could imply code changes but does not directly ask for them, ask for confirmation before changing files.
